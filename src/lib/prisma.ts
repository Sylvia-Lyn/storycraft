// 这是一个模拟的Prisma客户端，用于构建过程
// 在实际开发中，应该使用真正的Prisma客户端

// 创建一个模拟的Prisma客户端对象
export const prisma = {
  work: {
    findMany: async () => [],
  },
  characterScript: {
    findMany: async () => [],
    findUnique: async () => null,
    update: async () => ({}),
    delete: async () => ({}),
  },
};
